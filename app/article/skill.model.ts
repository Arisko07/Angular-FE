export class Skill{

    constructor(public icon: string, public name: string, public type: string, public description: string){
        this.icon = icon;
        this.name = name;
        this.type = type;
        this.description = description;
    }
}